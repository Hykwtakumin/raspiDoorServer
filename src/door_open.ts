
import ArduinoFirmata from 'arduino-firmata'
import _ from "lodash"

function doorOpen(linda : any) : void {
    const config = linda.config;
    const tupleSpace = linda.tuplespace(config.linda.space)
    
}